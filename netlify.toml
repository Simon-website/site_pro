[build]
  publish = "."
  functions = "netlify/functions"

[dev]
  port = 3000

# Redirections API → Netlify Functions
[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/:splat"
  status = 200
  force  = true
